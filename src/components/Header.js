import React from "react";
import '../css/Header.css';

const Header = () => {
    return (
        <p className="header">Logo, Sign-up and Sign-in links and intro content (background image, title, message and search bar) will appear here.</p>
    )
}

export default Header;